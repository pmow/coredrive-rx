# CoreDrive RX v1.10.0

- **Region discovery now asks the repeater it just heard**, instead of whichever candidate
  a timer happened to pick. Asking only works zero-hop direct, and this receiver is
  moving: a repeater chosen a minute later may already be out of range. The timer remains
  as the fallback for a candidate heard once and never heard again.
